age = input("Enter the age");
print(age)

# input function is used to take input from user , but it always return value as string therefore we need to type cast it to the datatype we want.

age = int(input("Enter the age :"))

print(age)